# Application PORT
server.port= 8080
# ===============================
# = DATA SOURCE
# ===============================
# configurations for the database connection
spring.datasource.url=jdbc:postgresql://localhost:5432/iconnect
spring.datasource.username=iconnect_user
spring.datasource.password=P@ssw0rd
spring.datasource.driver-class-name=org.postgresql.Driver
# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle=true
spring.datasource.validationQuery=SELECT 1

# ===============================
# = JPA / HIBERNATE
# ===============================
# Show or not log for each sql query
spring.jpa.show-sql=true
# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto=update
# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ===============================
# = ICONNECT / PROPERTIES
# ===============================
iconnect.app.jwtSecret=${JWT_SECRET:Y2whcXUzanZ2dHMzY3IzdA==}
iconnect.app.jwtExpirationMs=${JWT_EXPIRATION_MS:86400000}
iconnect.app.default.admin.email=${DEFAULT_ADMIN_EMAIL:admin@satra.io}
iconnect.app.default.admin.password=${DEFAULT_ADMIN_PASSWORD:admin@2023P$$w0rd}
iconnect.app.default.aes.password=${DEFAULT_AES_PASSWORD:78666}
iconnect.app.env=${ENV:dev}

# ===============================
# = TIME FORMAT
# ===============================
spring.mvc.format.date=yyyy-MM-dd
spring.mvc.format.date-time=yyyy-MM-dd HH:mm:ss
spring.mvc.format.time=HH:mm:ss

